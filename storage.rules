rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Phase 2: receipt uploads. Locked down until then.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
