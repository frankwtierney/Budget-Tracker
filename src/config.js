// App-wide constants. APP_NAME is the only place the product name lives.
export const APP_NAME = 'BudgetHub';

// Email domain allowlist. Add addresses or domains as needed.
// Format: full email address OR '@domain.edu' for domain-wide access.
export const EMAIL_ALLOWLIST = [
  '@buffalo.edu',
];
